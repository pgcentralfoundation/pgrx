mod tests {
    #[allow(unused_imports)]
    use crate as pgx_tests;

    use pgx::*;

    #[pg_test]
    fn test_alloc_node_by_type() {
        //        let node = PgNode::IndexAmRoutine.new();
    }
}
