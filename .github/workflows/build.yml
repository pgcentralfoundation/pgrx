name: cargo test --all

on:
  push:
    branches:
      - master
    pull_request:
      branches:
        - master

jobs:
  test:

    runs-on: ${{ matrix.os }}
    if: "!contains(github.event.head_commit.message, 'nogha')"

    strategy:
      matrix:
        version: [10, 11, 12]
        os: ["ubuntu-latest"]

    steps:
    - uses: actions/checkout@v1

    # which host are we running on?
    - name: hostname
      run: hostname

    # Ubuntu system dependencies required to build Postgres from sources
    - name: install postgres build dependencies
      run: sudo apt install -y bison flex zlib1g zlib1g-dev pkg-config libssl-dev

    # Install Postgres too
    - name: install Postgres
      run: sudo apt install -y postgresql-${{ matrix.version }}

    # install our cargo subcomment
    - name: install cargo pgx
      run: cd cargo-pgx && cargo install --path . --force

    # test our code
    - name: cargo test
      run: RUST_BACKTRACE=1 CARGO_TARGET_DIR=~/cargo-target-dir cargo test --all --features pg${{ matrix.version }} --no-default-features

    # make sure the examples build too, using 'pgx'
    - name: build arrays example
      run: CARGO_TARGET_DIR=~/cargo-target-dir cd pgx-examples/arrays && sudo cargo pgx install
    - name: build errors example
      run: CARGO_TARGET_DIR=~/cargo-target-dir cd pgx-examples/errors && sudo cargo pgx install
    - name: build srf example
      run: CARGO_TARGET_DIR=~/cargo-target-dir cd pgx-examples/srf && sudo cargo pgx install
    - name: build strings example
      run: CARGO_TARGET_DIR=~/cargo-target-dir cd pgx-examples/strings && sudo cargo pgx install
